# Secrets — never commit
.env
ssl/

# Local dev notes — contain private IPs, domains, prod paths (not for public repo)
CLAUDE.md
CLAUDE.local.md

# Runtime generated configs
sites-enabled/
AddOn/

# Test fixtures are committed (self-signed certs, dummy configs)
!tests/ssl/
!tests/sites-enabled/
!tests/AddOn/

# OS
.DS_Store
Thumbs.db
