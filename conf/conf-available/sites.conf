# User-Site configs — domain vhost definitions managed by the operator.
# Mount as a read-write volume in docker-compose:
#   - /data/proxy/sites:/etc/apache2/sites:rw
IncludeOptional /etc/apache2/sites/*.conf
