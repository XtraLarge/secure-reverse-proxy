<IfModule mod_cgid.c>
  # Debian defaults to ${APACHE_RUN_DIR}/socks/cgisock, which is awkward in
  # containers with aggressively dropped capabilities. /tmp stays writable.
  ScriptSock /tmp/apache2-cgisock
</IfModule>
